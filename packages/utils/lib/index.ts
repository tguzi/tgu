export function add(a: number, b: number): number {
  return a + b
}

export function paowuxian(x: number) {
  const a = 1,
        b = 2,
        c = 3
  if (!a) {
    return 0
  }
  return x * x * a + b * x + c
}
